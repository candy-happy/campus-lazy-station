# 私聊卖家修复 (2026-06-11T16:30)

## 根因
`git checkout -- app/js/wall.js` 回滚到原始版本，导致 `openChatWithId` 函数丢失。
- market.js 的 `chatWithSeller` 调用 `openChatWithId(res.conversation_id, res.other_phone, res.other_name)`
- 但 wall.js 中该函数不存在 → 点击无反应

## 修复
1. 在 wall.js 的 `openChatConv` 后面添加 `openChatWithId` 包装函数
2. 添加 `window.openChatWithId = openChatWithId` 导出

## 本次修改汇总
- **wall.js**: `openChatConv` 增强（document.title 诊断、页面切换、z-index:1001 压住子页面）；新增 `openChatWithId`
- **market.js**: `chatWithSeller` 去掉 `closeSubPage`，直接 overlay 聊天
- 版本号: app.html 中 js?v=3.0.37
