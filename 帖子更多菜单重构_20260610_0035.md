# 帖子更多菜单重构

## 目标
将分享、举报、删除三个次要操作收进统一 `⋯` 更多菜单，保持界面简洁。

## 修改文件

| 文件 | 改动 |
|------|------|
| `app/js/core.js` | 帖子流：卡片右上角加 ⋯ 按钮 + 底部 ⋯ 替代分享/举报/删除 |
| `app/js/wall.js` | 新增 `showPostMoreMenu()` + `closePostMoreMenu()` 函数；详情页同样改造；导出新函数 |
| `app/css/style.css` | 新增 `@keyframes postMoreIn` 动画 |

## 函数设计

`showPostMoreMenu(postId, event, postPhone)`:
- 在按钮右下角弹出固定定位的菜单
- 菜单项：📤 分享、🚩 举报（所有人可见）、🗑️ 删除（仅帖主）
- 点击外部自动关闭
- 传入 `postPhone` 判断是否为帖主

## 交互变化

**帖子流**：底部 `❤️赞 💬评论 ⋯`
**详情页**：右上角 `⋯` + 底部 `❤️赞 💬评论 ⋯更多`

## 版本
`v=3.0.19`，服务正常，无 JS 错误
