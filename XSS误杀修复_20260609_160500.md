# XSS 误杀导致校园墙数据丢失问题修复

## 问题描述
用户反馈"校园墙数据丢失"，页面无法显示内容。

## 根因分析
`middleware/requestLogger.js` 第 59 行的 XSS 检测正则表达式误杀正常请求：

```javascript
const xssPatterns = /(<script|javascript:|on\w+=)/i;
if (xssPatterns.test(req.url) || ...) { // 拦截 }
```

**问题**：`on\w+=` 匹配了 URL 参数 `phone=` 中的 `one=` 子串！
- `phone=` → 包含 `one=` → 匹配 `on\w+=` → 被判定为 XSS 攻击 → 请求被拒绝

### 日志证据
```
[SECURITY] XSS_ATTEMPT: {"url":"/api/wall/feed?phone=13645653760"}
[SECURITY] XSS_ATTEMPT: {"url":"/api/orders?phone=13645653760"}
[SECURITY] RATE_LIMIT_EXCEEDED: {"count":101}
```

所有带 `phone=` 参数的请求都被拦截，导致：
- 校园墙数据加载失败
- 订单页面加载失败
- 触发频率限制（大量被拦截请求）

## 修复方案
分离 URL 和 Body 的 XSS 检测规则：

```javascript
// URL 只检测真正危险的注入模式（不检测 on\w+=）
const xssUrlPattern = /(<script|javascript:)/i;

// Body 额外检测 HTML 事件属性（onclick=、onload= 等）
const xssBodyPattern = /(<script|javascript:|on\w+\s*=)/i;

if (xssUrlPattern.test(req.url) || xssBodyPattern.test(JSON.stringify(req.body || ''))) {
  // 拦截
}
```

## 验证结果
- ✅ `/api/wall/feed?tab=latest` → 返回 36 条帖子数据
- ✅ `/api/wall/feed?phone=13645653760` → 不再被误判为 XSS
- ✅ `/api/orders?phone=13645653760` → 正常访问
- ✅ PM2 已重启生效

## 修复文件
- `middleware/requestLogger.js` 第 57-65 行

## 时间
2026-06-09 16:05
