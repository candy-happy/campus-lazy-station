# 管理端校园墙举报管理 → 统一举报管理合并

## 目标
将管理端校园墙页面内的"举报管理"Tab 删除，统一汇聚到侧边栏的「🚫 举报管理」页面。

## 删除内容（3 文件）

### admin.html
- 校园墙管理 Tab 栏移除 `🚫 举报管理` 按钮（只保留帖子管理/评论管理）
- 移除整个 `wallPanel-reports` 面板（举报筛选按钮 + 表格 + 分页）

### admin/js/wall.js
- 删除 `filterWallReports()`、`loadWallReports()`、`handleWallReport()` 三个函数
- 删除 `_wallReportPage`、`_wallReportFilter` 全局变量
- 删除 4 个 window exports（`filterWallReports/loadWallReports/handleWallReport`）
- `switchWallTab` 移除 `reports` case 分支

### 后端
- 不动。已有 `POST /api/wall/report` 写 `wall_reports` + 同步写统一 `reports` 表
- 已有 `POST /api/reports/:id/handle` 统一处理含 wall 在内的所有来源举报

## 验证
- `admin/js/wall.js` 语法通过
- 侧边栏 `🚫 举报管理` → `page-reports` 入口未变
- 统一举报页已有 `📝 校园墙` 来源 Tab
