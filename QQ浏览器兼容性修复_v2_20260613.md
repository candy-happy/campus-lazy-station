# QQ/微信浏览器兼容性修复 v2 报告

**时间:** 2026-06-13  
**范围:** campus-lazy-station 全项目  
**目标:** 修复前端代码在 QQ/微信内置浏览器中的兼容性问题

## 本轮修复清单

### P0-严重（已修复）

| # | 问题 | 修复方案 | 影响文件 |
|---|------|----------|----------|
| 1 | Google Fonts 被墙阻塞页面加载 | 移除所有 `fonts.googleapis.com` 引用，统一降级字体链 | app.html, admin.html, rider.html, index.html (6处) |
| 2 | `accept="image/*"` QQ浏览器只能唤起相机/相册 | 移除所有 `accept` 属性，用户可自由选择文件来源 | app.html(8), admin.html(2), rider.html(1), user.js(2), market.js(1), teachers.js(1), wall.js(2), modals.js(1) = **17处** |
| 3 | `-webkit-overflow-scrolling: touch` 缺失 | `.sub-page` 和发现页横向滚动容器添加惯性滚动 | app/css/style.css (4处) |
| 4 | `::-webkit-scrollbar { display: none }` 降级缺失 | 为 `#wallChannels`, `#discoverHotActivities`, `#discoverHotClubs` 添加 WebKit 滚动条隐藏 | app/css/style.css (新增规则) |
| 5 | `Object.fromEntries()` 无 polyfill | admin/js/core.js 和 rider/js/core.js 添加功能检测+polyfill | 2个文件 |
| 6 | 搜索/聊天输入框缺少 `autocorrect` 和 `inputmode` | 8个输入框添加 `autocorrect="off"` + 搜索框 `inputmode="search"` + 数字框 `inputmode="numeric"` | app.html(5), rider.html(3) |
| 7 | `env(safe-area-inset-bottom)` 缺少 `constant()` 降级 | 3处刘海屏适配添加旧 iOS `constant()` fallback | app/css/style.css |
| 8 | `-webkit-text-size-adjust: 100%` 缺失 | 三端 body 均添加，防止微信缩放字体导致排版错乱 | app/admin/rider CSS |

### P1-中等（未修复，接受降级）

| # | 问题 | 风险等级 | 原因 |
|---|------|----------|------|
| 1 | `backdrop-filter` 不兼容 | 低 - 有固态背景色兜底 | 所有毛玻璃按钮/遮罩都有 `rgba()` 背景色，视觉效果降级但功能性完整 |
| 2 | `position: sticky` 键盘弹出失效 | 中 - 需 JS 重构 | 涉及所有页面顶栏+底部操作栏，需要 `focusin/focusout` 事件监听动态切换，风险较高留待下轮 |
| 3 | `100dvh` 单位兼容 | 低 - 已有 `height:100%` fallback | 旧浏览器回退到 `100%`，滚动高度基本可用 |
| 4 | `aspect-ratio` 不兼容 | 低 - 仅布局偏移 | 旧浏览器图片失去固定宽高比，样式略紊乱但功能正常 |
| 5 | `user-scalable=no` 限制 | 低 - 可能被微信忽略 | 部分小字号可能不可读，但微信多数版本已支持 |
| 6 | CSS `gap` flex间距 | 低 - 现代版本支持 | 141处使用，极老浏览器无间距但不影响核心功能 |
| 7 | `@media (hover: hover)` 触摸无效 | 无影响 - 触摸设备天然无hover | 桌面端功能不受影响 |

### 已确认正确处理

- ✅ `-webkit-tap-highlight-color` 大量使用
- ✅ CSS变量 (--primary 等) 支持良好
- ✅ `-webkit-overflow-scrolling: touch` 多数滚动容器已有
- ✅ Login student_id 输入框已有 `inputmode="numeric"`
- ✅ 无 `import/export` 模块化、`??=` 等高风险 JS 语法
- ✅ async/await 在现代微信/QQ浏览器中支持
- ✅ `IntersectionObserver` 微信较新版支持
- ✅ FormData/fetch 微信/QQ最新版均支持

## 修改文件清单

```
app/css/style.css       - 8处修改
app.html               - 13处修改
admin.html             - 2处修改
admin/css/style.css    - 1处修改
rider.html             - 4处修改
rider/css/rider.css    - 1处修改
app/js/user.js         - 2处修改
app/js/market.js       - 1处修改
app/js/teachers.js     - 1处修改
app/js/wall.js         - 2处修改
rider/js/modals.js     - 1处修改
admin/js/core.js       - 1处修改 (polyfill)
rider/js/core.js       - 1处修改 (polyfill)
```

## 后续建议

1. **`position: sticky` 键盘适配** - 下一个优先任务，影响用户输入体验
2. **`backdrop-filter` 移除/降级** - 可选，当前有兜底不影响功能
3. **真机 QQ 浏览器测试** - 建议在 Android QQ 浏览器中完整走一遍核心流程（登录→发帖→聊天→上传图片）
