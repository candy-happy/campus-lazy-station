# 发现页社团功能测试报告

## 测试时间
2026-06-10 23:35 ~ 23:58

## 发现的 Bug

### 1. clubs 表缺少 activity_count 列
- **现象**: `GET /api/clubs/ranking` 报错 `no such column: activity_count`
- **影响**: 排行接口完全不可用；社团列表排序 `sort=active` 也会失败
- **修复**: 执行 `ALTER TABLE clubs ADD COLUMN activity_count INTEGER DEFAULT 0`
- **commit**: `54aedba`

### 2. activity_count 从未被更新
- **现象**: 即使列存在，值始终为 0，排行无意义
- **修复**: 在 `routes/activities.js` 的创建活动和取消/删除活动路由中添加了 `UPDATE clubs SET activity_count = (SELECT COUNT(*) ...)` 同步逻辑
- **commit**: `54aedba`

## 审查确认无问题的部分

### 后端
- ✅ 社团列表 sort 参数 (hot/new/active) — SQL ORDER BY 逻辑正确
- ✅ 排行 API — 按 member_count DESC 排序，Top N 限制正确
- ✅ 活动日历 — 使用 start_date/end_date 参数，前端已对接
- ✅ 公告发布/删除/置顶 — 所有 API 实现完整

### 前端 (discover.js)
- ✅ 语法检查通过 (node -c)
- ✅ 所有 onclick 函数均已定义并导出到 window
- ✅ 社团海报 Canvas 绘制 (drawClubPoster) — 渐变背景+白色卡片+文字排版
- ✅ 活动海报 Canvas 绘制 (drawActivityPoster) — 暖色渐变+信息字段
- ✅ 海报保存下载 (downloadPoster)
- ✅ 海报分享/复制 (sharePoster) — navigator.share → ClipboardItem → 兜底 download
- ✅ 日历视图 (renderCalendar) — 月历网格+活动圆点+点击展开
- ✅ 社团排行面板 (toggleClubRanking) — 异步加载+奖牌+跳转

### HTML 结构 (app.html)
- ✅ clubPostModal (公告弹窗) — 已添加
- ✅ posterModal (海报弹窗) — 已添加，含 canvas + 下载/分享按钮
- ✅ 社团管理弹窗、编辑弹窗、转让弹窗 — 全部存在

## 未发现问题的部分
- 社团详情页「📤 分享海报」按钮 — 正确调用 openClubPoster()
- 活动详情页「📤 分享海报」按钮 — 正确调用 openActivityPoster()
- 公告删除按钮权限判断 — owner/admin/本人均可删
- 社团管理面板 Tab 切换 — 统计/编辑/转让/解散功能完整

## Commit 记录
| commit | 内容 |
|--------|------|
| 5d43e84 | 入社审批+成员管理 |
| c23a121 | 公告/动态墙 |
| dc6f837 | 管理后台 |
| dc2c978 | 活动日历视图 |
| fc44299 | 社团排行+排序 |
| 47b96c9 | 海报生成 |
| 54aedba | 修复 activity_count 列 |

## 服务状态
- PM2: online, 零错误日志
- 所有 API 返回正常
