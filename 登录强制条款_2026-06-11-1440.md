# 登录页强制勾选服务条款功能实施

## 目标
将服务条款和隐私协议添加到登录页面，强制用户勾选"我已阅读并同意"后才能登录。

## 修改内容

### 1. 新建 `public/terms.html`
- 完整的服务条款 + 隐私协议 HTML 页面
- Tab 切换设计（服务条款/隐私协议）
- 移动端响应式排版
- 通过 postMessage 与父窗口通信（关闭、切换 Tab）

### 2. 修改 `app/js/user.js`
改动集中在 `showLoginPage()` 和 `doLogin()` 附近：

**登录页 UI 变更:**
- 原: `<p>登录即表示同意《用户协议》</p>`（无实际约束）
- 新: checkbox + "我已阅读并同意《服务条款》和《隐私协议》"链接

**新增函数:**
- `updateLoginBtn()`: 根据 checkbox 状态禁用/启用登录按钮
- `showTermsModal(type)`: 全屏 iframe 弹窗展示条款，支持 'terms'/'privacy' 切换
- `closeTermsModal()`: 清理弹窗和事件监听器

**登录验证:**
- `doLogin()` 新增 `if (!document.getElementById('agreeTerms').checked)` 拦截

**初始状态:**
- 登录页显示时按钮默认禁用（opacity: 0.5），勾选后恢复

## 交互流程
1. 用户打开登录页 → checkbox 未勾选 → 登录按钮禁用
2. 点击《服务条款》/《隐私协议》链接 → 全屏弹窗 iframe 展示完整条款
3. 弹窗内可切换 Tab 阅读两个文档，点击 ✕ 关闭
4. 勾选同意后 → 登录按钮激活 → 可正常登录
5. 未勾选时点登录 → toast "请先阅读并同意服务条款和隐私协议"

## 待处理
- 文档中 4 个 `【待填写】` 占位符（运营主体名称、联系邮箱、隐私邮箱、联系地址）
- 骑手端（rider.html）和管理端（admin.html）如需同样处理
