# 校园墙分享功能修复

**日期**: 2026-06-10
**版本**: wall.js v3.0.27 → v3.0.31

## 问题
用户报告：校园墙点击分享按钮，分享不生效（"没有分享"）。

## 根因分析

### Bug 1（主因）：`doSharePost` 错误检查逻辑错误

`doSharePost` 调用 `API.wallChat()` 后检查：
```js
if (!convRes.ok && convRes.code !== 'CHAT_002') { ... return; }
```

但后端 `/api/chat/wall-chat` 成功时直接返回 DB 对象 `{ id, user1_phone, ... }`，**没有 `ok` 字段**。
因此 `!convRes.ok` 永远是 `true`，导致所有情况都走进错误分支，分享从未真正执行。

**对比**：`tryWallChat` 中正确使用了 `if (res.error)` 检查。

**修复**：
```diff
- if (!convRes.ok && convRes.code !== 'CHAT_002') {
-   const errMsg = convRes.code === 'CHAT_PRIVACY_BLOCKED'
-     ? '📤 分享失败：' + convRes.error
-     : (convRes.error || '无法发起会话');
-   showToast(errMsg);
+ if (convRes.error) {
+   showToast('📤 分享失败：' + convRes.error);
    return;
  }
- const convId = convRes.conversation_id || convRes.id;
+ const convId = convRes.id;
```

### Bug 2（次要）：`closePostMoreMenu` 未导出到 window

`showPostMoreMenu` 的 `⋯` 菜单中"分享"、"举报"、"删除"等动作通过 `onclick` 内联 HTML 调用 `closePostMoreMenu()`，但该函数仅定义在 wall.js IIFE 闭包内，未导出到 `window`，导致每次点击产生 ReferenceError。

**修复**：在 window exports 区域添加 `window.closePostMoreMenu = closePostMoreMenu;`

## 变更文件
- `app/js/wall.js`：修复 doSharePost 错误检查 + 导出 closePostMoreMenu
- `app.html`：wall.js 缓存版本号 3.0.27 → 3.0.31