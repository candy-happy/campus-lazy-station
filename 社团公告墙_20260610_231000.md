# 社团公告/动态墙 — 2026-06-10

## 目标
为社团添加公告/动态墙功能，社长和管理员可发布公告，所有成员可在详情页查看。

## 改动

### 数据库
- `scripts/migrate-club-posts.js` — 新建 `club_posts` 表
  - id, club_id, phone, content, images(JSON数组), pinned(置顶), created_at

### 后端 routes/clubs.js
- `POST /:id/posts` — 发公告（owner/admin），支持最多4张图片
- `GET /:id/posts` — 获取公告列表（置顶优先，时间倒序），关联用户名
- `DELETE /:id/posts/:postId` — 删除公告（owner/admin 或本人）
- `PUT /:id/posts/:postId/pin` — 置顶/取消置顶
- 社团详情 `GET /:id` 返回值新增 `posts` 字段（最新5条）

### 前端 api.js
- getClubPosts / createClubPost / deleteClubPost / pinClubPost

### 前端 app/js/discover.js
- 社团详情页新增「📢 社团公告」区块
  - 公告卡片：作者、时间、置顶标记、内容、图片预览
  - 删除按钮（owner/admin/本人可见）
  - 空状态：「暂无公告」
- 社长/管理员可见「✏️ 发布公告」按钮 → 弹出发布弹窗
- openClubPostModal / submitClubPost / deleteClubPost

### 前端 app.html
- 新增 `clubPostModal`：正文输入 + 图片上传（最多4张）

## Git
- commit: `c23a121` — feat: 社团公告/动态墙功能
- 前序commit: `5d43e84` — feat: 社团入社审批机制+成员管理面板
- Push待代理恢复

## 第2步完成，用户视角效果
- 进入社团详情 → 看到公告列表（置顶优先）
- 社长/管理员 → 点"发布公告" → 写内容+传图 → 发布
- 任何公告作者或管理员 → 点🗑️删除
