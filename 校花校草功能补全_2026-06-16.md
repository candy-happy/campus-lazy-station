# 校花校草模块功能补全 — 2026-06-16

## 需求
1. 评论功能（候选人详情页）
2. 分享按钮
3. 删除按钮（自己的参赛记录 / 自己的评论）
4. 点赞规则：每人每天限投3人，同一人只能被投一次

## 改动文件

### 1. `config/database.js` — 新增 `star_comments` 表
- 5个字段：id, candidate_id, phone, content, created_at
- 索引：idx_star_comments_candidate

### 2. `routes/campus-star.js` — 后端改造

| 接口 | 方法 | 变更 |
|------|------|------|
| `/vote` | POST | 从"1票/天"改为"3票/天+不可重复投同一人" |
| `/my-status` | GET | 返回 `voteCountToday`(今日已投数) + `votedFor`(已投ID数组) |
| `/candidate/:id` | DELETE | **新增** — 删除自己的参赛记录(级联删评论+投票) |
| `/comments/:candidateId` | GET | **新增** — 候选人评论列表(含昵称+头像) |
| `/comments` | POST | **新增** — 发表评论(≤500字) |
| `/comments/:id` | DELETE | **新增** — 删除自己的评论 |

### 3. `app/js/campus-star.js` — 前端全量重写

- 状态变量：`_starVoteCountToday`(数字) + `_starVotedFor`(数组)，替代原来的布尔值
- 候选人卡片：顶部显示"今日已投 X/3 票"，每个候选人的投票按钮区分状态(可投/已投/满票)
- 候选人详情页：
  - 顶栏：分享按钮(📤) + 删除按钮(🗑，仅自己可见)
  - 投票按钮：显示状态 + 剩余票数
  - 评论区：输入框 + 评论列表(头像/昵称/内容/时间) + 删除(自己评论)
- 新增函数：`shareStarCandidate`, `deleteStarEntry`, `loadStarComments`, `submitStarComment`, `deleteStarComment`
