# 社团入社审批机制实现 - 2026-06-10

## 目标
将社团"即时加入"改为"申请→审批"流程，并添加社团管理面板。

## 改动清单

### 数据库
- `scripts/migrate-club-applications.js` — 新建 `club_applications` 表
  - 字段: id, club_id, phone, reason, status(pending/approved/rejected), reviewed_by, reviewed_at, created_at
  - UNIQUE(club_id, phone)
  - 将现有 member 角色成员迁移为 approved 记录

### 后端 routes/clubs.js
1. `POST /:id/join` — 改即时加入为提交申请，写入 club_applications 表，通知社长
2. `GET /:id/applications` — 社长/管理员查看申请列表（支持 status 筛选）
3. `POST /:id/applications/:appId/approve` — 审批通过，写入 club_members + 通知申请人
4. `POST /:id/applications/:appId/reject` — 拒绝申请，通知申请人
5. `GET /:id` — 详情接口新增返回 `my_role` 和 `my_app_status` 字段
6. `DELETE /:id/members/:phone` — 踢出成员（社长/admin权限分级）
7. `PUT /:id/members/:phone/role` — 社长修改成员角色（升降管理员）

### 前端 api.js
- `joinClub(id, reason)` — 支持传入申请理由
- `getClubApplications(id, status)` — 获取申请列表
- `approveClubApplication(clubId, appId)` — 审批通过
- `rejectClubApplication(clubId, appId)` — 拒绝申请
- `kickClubMember(clubId, phone)` — 踢出成员
- `updateMemberRole(clubId, phone, role)` — 修改角色

### 前端 app/js/discover.js
- `showClubDetail()` — 根据 my_role/my_app_status 显示不同按钮：
  - 社长/管理员: "社团管理"入口
  - 已审批/待审批: "审批中"禁用按钮
  - 被拒绝: "重新申请"
  - 非成员: "申请加入"
- `applyJoinClub(id)` / `submitClubApply()` — 申请弹窗（含理由输入）
- `showClubManagePanel()` — 社团管理子页面（两个Tab）:
  - 📋 入社申请：审批列表（通过/拒绝按钮）
  - 👥 成员管理：列表（踢出/升降角色）

### 前端 app.html
- 新增 `clubApplyModal` — 申请加入弹窗（理由输入+提交）
- 新增 `clubManageSub` — 社团管理子页面容器

## 提交
- commit: `5d43e84` — feat: 社团入社审批机制+成员管理面板
- GitHub push: 失败（代理 127.0.0.1:7890 不可达），待重试

## 验证
- API `/api/clubs` 正常返回社团列表
- routes/clubs.js 语法通过
- discover.js 语法通过
- api.js 语法通过
