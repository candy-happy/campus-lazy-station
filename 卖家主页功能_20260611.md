# 卖家卡片可点击 - 卖家主页功能

**时间**: 2026-06-11 10:55
**目标**: 商品详情页卖家卡片点击后跳转到卖家主页，展示该卖家所有在售商品

## 实现

### app.html
- 新增 `sellerItemsPage_sub` 子页面：
  - `sellerInfoCard` — 卖家头像 + 名称 + 私聊按钮
  - `sellerItemsContainer` — 商品网格（2列，复用 market-card 卡片样式）

### market.js
- `renderItemDetail` 卖家卡片 onclick 从 `event.stopPropagation()` 改为 `viewSeller(phone, name, avatar)`
- 新增 `viewSeller(phone, name, avatar)`：渲染卖家信息卡 → 打开子页面 → 调 loadSellerItems
- 新增 `loadSellerItems(sellerPhone)`：调 `API.getMarketItems({seller: phone})` → 渲染卡片网格；空结果时显示「该卖家暂无在售商品」
- `viewSeller` + `loadSellerItems` 已导出到 window

### style.css
- `.item-seller-card` 加 `cursor: pointer` + `transition: transform 0.15s` 点击反馈

### 后端
- `GET /api/items?seller=xxx` 已有支持（routes/market.js 第87行）

## 依赖确认
- `renderAvatarHtml` 在 core.js 中定义并导出 window
- `escHtml` 在 core.js 中定义
- 后端 `seller` 参数过滤逻辑：`WHERE mi.seller_phone = ?`
