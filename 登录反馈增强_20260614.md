# 登录反馈增强 & BOM清理 & 双重限流修复 (2026-06-14 16:07)

## 问题
用户反馈：本地无法登录 + 密码错误无反馈 + 需确认登录频次限制

## 根因
1. **api.js + user.js 带 UTF-8 BOM** → 浏览器 JS 解析失败 → `API.userLogin` 不存在 → 登录按钮完全无效
2. 8个文件有BOM残留（包括 server.js / style.css / rider.js 等）

## 修改清单

### 1. BOM 清理 (10个文件)
- `api.js` — 前端API桥接层，BOM导致全部API方法不可用
- `app/js/user.js` — 登录表单逻辑，BOM导致doLogin/refreshCaptcha等函数未定义
- `app/css/style.css` — CSS首规则可能被跳过
- `admin/css/style.css`
- `promo/index.html` / `public/promo/index.html`
- `rider/css/rider.css` / `rider/js/modals.js`
- `scripts/security-check.js`
- `server.js`

### 2. 登录错误反馈增强 (api.js + user.js + style.css)

**api.js (line 99):**
- 原来：`if (res.error) throw new Error(res.error);` — 丢失retryAfter和code
- 改为：`if (res.error) { const e = new Error(res.error); e.code = res.code; e.retryAfter = res.retryAfter; throw e; }`

**user.js doLogin() catch块:**
- 密码错误 → 密码框红色边框 + shake动画 + 清空 → showToast("密码错误，请重试")
- 首次登录密码错误 → showToast("账号不存在，首次登录请使用默认密码 shoujihao，请重试", 4500ms)
- 验证码错误 → 验证码框红色边框 + shake + 自动刷新 → showToast("验证码错误，请重新输入")
- 限流(RATE_001) → showToast("登录尝试过多，请 N 秒后重试", 4000ms)
- 其他错误 → showToast(errorMessage || "网络异常，请重试")

**style.css:**
- 新增 @keyframes shake 动画

### 3. 错误码修正 (routes/auth.js)
- 3处密码错误：`FORBIDDEN(SYS_006)` → `PASSWORD_WRONG(USR_004)`
- 修改密码旧密码不正确：同样改为 `USR_004`

### 4. 双重限流修复 (middleware/rateLimit.js)
- 全局限流 `classify()` 不再对 `/api/` 登录路由重复计数
- 原来：`url.includes('/login')` 匹配 → 登录路由被计两次（全局+路由级）
- 改为：`url.includes('/login') && !url.startsWith('/api/')` → 只拦截非API登录页面

## 限流配置现状
- 用户登录：路由级 5次/分钟/IP（API返回 retryAfter + 前端展示倒计时）
- 骑手登录：路由级 5次/分钟/IP
- 管理员登录：有 bruteForceGuard (5次失败封IP 15分钟)
- 全局浏览：300次/分钟/IP

## 验证结果
- ✅ 错误密码 → 400, `{"error":"密码错误","code":"USR_004"}`
- ✅ 正确登录 → 200, 返回token和用户信息
- ✅ 验证码错误 → 400, `{"error":"验证码错误或已过期","code":"SYS_001"}`
- ✅ 请求频繁 → 429, `{"error":"请求过于频繁","code":"RATE_001","retryAfter":60}`
