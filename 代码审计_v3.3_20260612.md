# 校园圈 v3.3 代码审计报告

**审计时间**: 2026-06-12 16:31  
**审计范围**: 全量代码（server.js + config + 中间件 + 工具函数 + 路由 + 前端 API 桥接层）  
**审计人**: AI 代码审查

---

## 一、审计概览

| 级别 | 数量 | 说明 |
|------|------|------|
| 🔴 Critical | 2 | 必须立即修复 |
| 🟠 High | 3 | 应尽快修复 |
| 🟡 Medium | 4 | 建议修复 |
| 🟢 Low | 4 | 可选优化 |

---

## 二、🔴 Critical 问题

### C-1: 重复限流系统 — requestLogger.js 与 rateLimit.js 各自维护独立滑动窗口

**文件**: `middleware/requestLogger.js` vs `middleware/rateLimit.js`  
**严重性**: Critical  
**影响**: 双份内存消耗、限流配置不一致、状态码混乱（403 vs 429）

**问题描述**:
`requestLogger.js` 内部实现了完整的滑动窗口限流逻辑（`checkSlidingWindow` + `detectSuspicious`），与 `rateLimit.js` 完全独立。这导致：

1. **双份内存存储**: `suspiciousIPs`（requestLogger）和 `windows`（rateLimit）各存一份 IP 时间戳数组
2. **硬编码限制**: requestLogger 的 `RATE_LIMITS` 是硬编码的，不受 `config/index.js` 控制
   - 最明显的差距：`upload: { max: 300 }`（requestLogger）vs `upload: { max: 10 }`（rateLimit config 默认）
3. **不同状态码**: requestLogger 返回 403，rateLimit 返回 429（RFC 6585 标准）
4. **先执行拦截**: requestLogger 在中间件链中先于 rateLimit 注册，等于第一道限流关

**修复建议**:
```diff
// middleware/requestLogger.js — 将 RATE_LIMITS + checkSlidingWindow + detectSuspicious 中的限流逻辑移除
// 只保留纯安全检测（SQL注入/XSS/路径遍历），不再做限流计数
- const RATE_LIMITS = { ... };      // 删除
- function checkSlidingWindow() {}   // 删除
// detectSuspicious 只保留安全攻击检测部分，移除 RATE_LIMIT_EXCEEDED 分支
```

---

### C-2: CORS 默认配置 `'*'` 在 credentials 模式下不生效

**文件**: `config/index.js:44` + `server.js:18-36`  
**严重性**: Critical（如果未来前端与 API 分离部署）

**问题描述**:
```javascript
// config/index.js
CORS_ORIGINS: process.env.CORS_ORIGINS ? process.env.CORS_ORIGINS.split(',') : '*',
```
当 `CORS_ORIGINS` 环境变量未设置时，默认值为字符串 `'*'`。这导致：
```javascript
// server.js — 三元表达式将 '*' 视为真值，不做 split 为数组
const allowedOrigins = config.CORS_ORIGINS  // ← 为 '*'
    ? config.CORS_ORIGINS.split(',').map(s => s.trim())  // ['*']
    : [...硬编码列表];

// 回调中: allowedOrigins.includes(origin) → ['*'].includes('http://real-origin.com') → false
```
CORS 规范不允许 `credentials: true` 与 `*` 通配符同时使用。当前生效是因为：
- 同一服务器部署（无跨域 Origin 头）→ `!origin` 条件通过
- `NODE_ENV === 'development'` → 全部放行

**修复建议**:
```javascript
// config/index.js — 默认值改为空字符串，让 server.js 走硬编码兜底
CORS_ORIGINS: process.env.CORS_ORIGINS || '',
```

---

## 三、🟠 High 问题

### H-1: requestLogger SQL 注入检测正则可能误伤用户内容

**文件**: `middleware/requestLogger.js:118`  
**影响**: 用户发布包含 SQL 关键词的帖子/评论时可能被 403 拦截

**问题**:
```javascript
const sqlPatterns = /(\b(SELECT|INSERT|UPDATE|DELETE|DROP|UNION|ALTER)\b.*(--|;|\/\*|\*\/))/i;
if (sqlPatterns.test(JSON.stringify(req.body || ''))) { /* 403 */ }
```
- 帖子内容 "SELECT 了一门课程--很有意思" → 触发（SELECT + --）
- JSON.stringify 会将整个 body 对象（含 content/phone/nickname 等多字段）拼接测试

**修复建议**:
- 仅对 URL 参数做 SQL 注入检测（URL 中出现 SQL 关键词才是真正可疑的注入尝试）
- 或缩小正文检测范围：仅检测 `' OR`、`1=1`、`UNION SELECT` 等典型注入模式

---

### H-2: 数据库文件可能提交到 Git

**文件**: `scripts/lazy_station.db`（32KB）  
**状态**: `.gitignore` 已排除 `*.db`，此文件未被追踪 ✅  
**风险**: 如果 `.gitignore` 规则被意外修改或删除，32KB 的测试数据库可能被提交

**建议**: 确认 `scripts/lazy_station.db` 是故意保留的测试数据；将 `scripts/*.db` 也加入 `.gitignore`

---

### H-3: 生产环境 CSP 缺少 Nginx 代理的域名

**文件**: `middleware/security.js:27-35`  
**现状**: CSP 的 `img-src` 和 `connect-src` 仅包含 `'self'` + localhost  
**分析**: 当前通过 `'self'` 覆盖了同源场景（访问 `http://服务器IP`），Nginx 反代下客户端看到的是 Nginx 的 origin，API 请求也走同源——所以**目前可用**。但未来如果：
- 使用 CDN 分发静态资源
- 使用对象存储存放上传图片
- 接入第三方登录/支付页面

则需更新 CSP 白名单。

**建议**: 在 `.env` 中添加 `CSP_EXTRA_ORIGINS` 环境变量，按需扩展。

---

## 四、🟡 Medium 问题

### M-1: `public/terms.html` 联系方式占位符未填写

**位置**: 第 194 行（服务条款）和第 330 行（隐私协议）  
**内容**: `电子邮箱：【待填写】`、`联系地址：【待填写】`  
**说明**: 法规合规性要求，正式上线前应填写真实联系方式。

---

### M-2: 空 `.db` 文件残留

**文件**: `campus.db`（0字节）、`data.db`（0字节）  
**建议**: 删除这两个 0 字节文件，避免误导。

---

### M-3: `resolveAvatar` 函数引用未在本文件中定义

**文件**: `routes/wall.js` 中 `feed` 路由使用了 `resolveAvatar(p.phone, p.avatar, avatarMap)`  
**说明**: 该函数定义不在 `wall.js` 中，需确认已正确 `require` 或在 `routes/` 共享模块中导出。

---

### M-4: rateLimit.js 中 cleanupTimer.unref() 可能被误用

**文件**: `middleware/rateLimit.js:62`  
```javascript
cleanupTimer.unref();
```
**说明**: `unref()` 使定时器不阻止进程退出——这本身是好的。但如果 PM2 的 `wait_ready` 或优雅关闭依赖于所有异步操作完成，定时器的存在/不存在可能导致不一致行为。当前无实际影响，仅为代码审查提示。

---

## 五、🟢 Low 问题

### L-1: `requestLogger.js` 日志轮转使用 fs.statSync 可能阻塞

```javascript
const stat = fs.statSync(ACCESS_LOG, { throwIfNoEntry: false }); // 同步阻塞
```
10MB 轮转阈值下，statSync 耗时极短（<1ms），实际影响可忽略。未来如果需要异步优化，改用 `fs.promises.stat`。

### L-2: 前端 `api.js` 中大量链式 `.then()` 可用 async/await 统一

无功能影响，纯代码风格。

### L-3: `server.js` 中 uploads 清理函数的 IIFE 递归 setTimeout 可用 `setInterval`

当前用递归 `setTimeout`（24h 一次），功能正常，但 `setInterval` 语义更清晰。

### L-4: `middleware/requestLogger.js` 中 `suspiciousIPs` Map 清理间隔 5 分钟与 rateLimit 的 5 分钟一致但使用不同定时器

两个 5 分钟定时器做类似的事情，可合并为一个清理模块。

---

## 六、已验证安全措施 ✅

以下安全措施已确认到位，无需修改：

| 措施 | 状态 |
|------|------|
| JWT 自定义 base64url 编码 + HMAC-SHA256 签名 | ✅ |
| bcrypt 管理员密码哈希 | ✅ |
| 5 级认证中间件（optionalAuth/requireAuth/requireAdmin/requireRider） | ✅ |
| 骑手冻结 30s 轮询缓存 + Token 黑名单 | ✅ |
| 文件上传魔数校验（JPEG/PNG/GIF/WebP/MP4/WebM） | ✅ |
| 文件类型白名单 + 20MB 限制 | ✅ |
| 路径遍历防护（uploads 中间件 + sanitizePath 工具） | ✅ |
| 输入清洗（sanitizeString/sanitizeNumber/isValidPhone/isValidEmail） | ✅ |
| 安全响应头（CSP/X-Frame-Options/X-Content-Type-Options/Referrer-Policy） | ✅ |
| 移除 X-Powered-By | ✅ |
| SVG 验证码（4位数字 + 干扰线 + 噪点 + 旋转 + 一次性） | ✅ |
| 5 级滑动窗口限流（login/post/upload/admin/browse） | ✅ |
| 慢请求告警（>1s） | ✅ |
| 日志自动轮转（10MB） | ✅ |
| uncaughtException + unhandledRejection 处理 | ✅ |
| SIGINT/SIGTERM 优雅关闭（db.close） | ✅ |
| .env 在 .gitignore 中 | ✅ |
| AI 审核 4 入口（校园墙/评论/商品/商品评论） | ✅ |

---

## 七、修复优先级建议

1. **立即**: C-1（合并 requestLogger 限流到 rateLimit.js）— 改动小、影响明确
2. **本次迭代**: C-2（CORS 默认值修正）+ H-1（SQL 注入检测优化）
3. **下个迭代**: H-2（scripts/*.db gitignore 加固）+ M-1（terms.html 联系方式）
4. **可延后**: L-1 到 L-4
