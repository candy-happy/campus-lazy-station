# 学号+密码登录改造 — 2026-06-12

## 需求
用户端登录从手机号+验证码改为学号（9位）+密码，默认密码 `shoujihao`，首次登录自动注册，设置页添加修改密码。

## 改动文件清单

| 文件 | 改动说明 |
|------|---------|
| `config/database.js` | `users` 表: `phone` → 去掉 NOT NULL、新增 `student_id TEXT UNIQUE`、`password TEXT`；启动时 ALTER TABLE 迁移 |
| `routes/auth.js` | 重写 `POST /api/user/login`（学号+密码）；新增 `POST /api/user/change-password` |
| `utils/jwt.js` | JWT verify: `data.type !== 'admin'` 时允许 `student_id` 替代 `phone` |
| `utils/response.js` | 新增 `PASSWORD_WRONG: 'USR_004'` |
| `api.js` | `userLogin(name,phone,captcha)` → `userLogin(student_id,password)`；新增 `changePassword(old,new)`；`restoreSession` 返回 `student_id` |
| `app/js/user.js` | 登录页：学号+密码输入框替代手机+验证码；设置页：增加"🔑 修改密码"弹窗；session 恢复兼容 student_id |
| `app/js/core.js` | `fmtPhone` 对非11位手机号直接返回原文（兼容学号） |

## 核心逻辑

1. **首次登录**：学号+默认密码 `shoujihao` → `phone=student_id` 创建用户 → 注册奖励积分 → 提示修改密码
2. **老用户首次迁移**：无 `password` 字段 → 用默认密码校验 + 自动写入 bcrypt hash
3. **修改密码**：验证旧密码 → bcrypt 写入新 hash

## 向后兼容设计

- 新用户 `phone` 列 = `student_id`（9位数字），满足 UNIQUE 约束
- 所有模块（订单/积分/通知/校园墙等）通过 `currentUser.phone` 查询，值 = student_id，查找一致
- `fmtPhone` 对9位学号返回原文（正则不匹配不脱敏）
- JWT payload: `{ type: 'user', student_id, phone }`，双字段确保兼容

## 未修改部分

- 骑手登录保持不变（UID+学号+手机号）
- 管理端登录保持不变
- 验证码系统保留（`utils/captcha.js`、`GET /api/captcha`）供后续使用
