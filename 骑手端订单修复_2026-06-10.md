# 骑手端订单数据消失 - 修复报告

**时间：** 2026-06-10 08:32-08:42  
**版本：** v3.0.27 → v3.0.28

## 根因

模块化拆分（commit `882f4a5`，rider.html 1829行→260行+6JS模块）时，**两处关键代码遗漏**：

### 1. 初始化 IIFE `initRider()` 缺失
原始 `rider.html` 中的 `(function initRider() { ... })()` 未迁移到任何 JS 模块。
后果：页面加载后不会从 localStorage 恢复登录会话、不自动加载订单、不显示登录页。

### 2. 6个共享常量/函数定义缺失
原始内联定义的变量在拆分后未被包含：
- `fmtTime(ts)` - 时间格式化
- `statusColor` - 状态颜色映射
- `statusIcon` - 状态图标映射
- `statusLabel` - 状态文本映射
- `typeEmoji` - 服务类型表情
- `serviceNames` - 服务类型名称

这些变量被 `orders.js`（`renderRiderOrders`/`showOrderDetail`）、`wallet.js`（收入明细渲染）、`chat.js`（聊天标题）共同使用。缺失导致首次调用时 `TypeError` → 被 `refreshOrders` 的 try/catch 捕获 → 订单列表始终空状态。

## 修复内容

| 文件 | 修改 |
|------|------|
| `rider/js/core.js` | 添加 `await refreshOrders()`（登录时） + `initRider()` IIFE |
| `rider/js/orders.js` | 顶部添加 6 个常量定义 |
| `rider.html` | 版本号 v3.0.27 → v3.0.28 |

## 验证

- API 直接测试：`/api/orders?rider_phone=13645653760&status=my` 返回 8 条订单 ✅
- HTTP 验证：core.js 包含 initRider ✅，orders.js 包含所有常量定义 ✅
- PM2：server 在线运行，PID 6420 ✅
