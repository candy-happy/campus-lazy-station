# 宣传页池州校园照片修复

**时间**: 2026-06-13 21:41

## 问题
`index.html` 宣传页面"美丽池院·温馨家园"板块，7张校园照片全部不显示（QQ/微信浏览器）。

## 根因
`.campus-card` 使用了 `aspect-ratio: 4/3`（大图 `16/9`），QQ/微信浏览器不支持该 CSS 属性。
- 卡片容器无显式 height → 高度塌为 0
- `img { height: 100% }` → 计算为 0px
- 图片虽成功加载（HTTP 200），但不可见

## 修复
```css
/* index.html */
.campus-card {
  aspect-ratio: 4/3;
  min-height: 220px; /* QQ浏览器 aspect-ratio 降级 */
}
.campus-card.large {
  aspect-ratio: 16/9;
  min-height: 250px; /* QQ浏览器 aspect-ratio 降级 */
}
```

## 验证
- 图片文件全部存在于磁盘（7张 jpg/jpeg/webp）
- 图片路径正确，服务器 static 正常返回
- 修复后服务端 HTML 已包含 min-height 降级
- 现代浏览器仍用 aspect-ratio，不受影响
