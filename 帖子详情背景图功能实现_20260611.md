# 帖子详情页背景图功能

## 时间
2026-06-11

## 需求
帖子详情界面顶部区域（原为橙色渐变），如果发帖人上传了个人资料背景图，则替换为背景图。

## 实现状态：已完成

此功能在之前对话中即已实现，本次检查确认全链路完整：

### 1. 后端 API（routes/wall.js L380-441）
- 端点：`GET /api/wall/user/:phone`
- 返回字段：`bg_image: user.bg_image || ''`
- 已正确查询 `users` 表的 `bg_image` 列

### 2. 前端 API 层（api.js L337）
```js
async wallUserProfile(phone) {
  return fetch('/api/wall/user/' + phone, { headers: this._headers() }).then(r => r.json());
}
```

### 3. 前端渲染（app/js/wall.js L566-574）
```js
var heroEl = document.getElementById("pdHero");
if (heroEl) {
  if (profile.bg_image) {
    heroEl.innerHTML = '<img src="' + profile.bg_image + '" style="width:100%;height:100%;object-fit:cover;display:block" />';
  } else {
    heroEl.innerHTML = '';
    heroEl.style.background = "linear-gradient(160deg, #FF6B2B 0%, #FF8F5E 35%, #FFB088 70%, #FFE0D0 100%)";
  }
}
```

### 4. 缓存版本号更新
- style.css: 3.0.34 → 3.0.35
- api.js: 3.0.32 → 3.0.33
- core.js: 3.0.38 → 3.0.39
- wall.js: 3.0.42 → 3.0.43
- market.js: 3.0.37 → 3.0.38

## 验证
- API 测试通过：用户 `13645653760` 返回 `bg_image: "/uploads/covers/cover-1781175346950-rrbr6a.jpg"`
- uploads/covers 目录有多个上传的背景图文件

## 注意
- 需硬刷新（Ctrl+F5）才能加载新版本 JS
- 未提交 git，等待用户决定是否提交
