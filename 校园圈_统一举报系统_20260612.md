# 校园圈 统一举报系统 20260612

## 目标
将散落在各模块的举报功能统一收敛到一张 `reports` 表 + 一个管理端页面。

## 已完成

### 1. 数据库层
- `config/database.js` → `reports` 表（id, source, target_type, target_id, target_content, reporter_phone, reason, detail, status[ pending|resolved|dismissed ], admin_note, created_at, handled_at）
- 索引：source+status, reporter_phone, created_at

### 2. 后端路由
| 端点 | 说明 |
|---|---|
| `GET /api/reports` | 统一举报列表，支持 source/status/page/limit 筛选 |
| `GET /api/reports/stats` | 各来源待处理数统计 |
| `POST /api/reports/:id/handle` | 处理（dismiss 驳回 / remove 删内容+标记已处理） |
| `POST /api/wall/report` | 校园墙举报（同步写入 wall_reports + reports） |
| `POST /api/market/report` | 二手市场举报 → reports |
| `POST /api/pets/report` | 猫狗日记举报 → reports |
| `POST /api/teachers/report` | 教师评价举报 → reports |
| `POST /api/clubs/report` | 社团举报 → reports |
| `POST /api/activities/report` | 活动举报 → reports |

### 3. 前端
- `admin/js/reports.js`：全新改写为统一举报管理（7个来源Tab + 4状态按钮 + 表格列表/分页 + 删除/驳回操作）
- `admin.html`：page-reports 区域改为 Tab 栏 + 状态按钮
- `admin/css/style.css`：source-tab.active / status-btn.active 激活样式

### 4. 服务端注册
- `server.js` 新增 `app.use('/api/reports', require('./routes/reports'))`

### 5. Bug修复
- `routes/teachers.js` 缺少 `requireAuth` import → 导致服务器启动崩溃（`ReferenceError: requireAuth is not defined`）→ 已补 `const { requireAuth } = require('../middleware/auth');`
- 同时补了 `makeError` 导入

### 6. AI审核入口（之前的session已完成）
- 私聊消息 `routes/chat.js` → POST /send
- 猫狗日记留言 `routes/pets.js` → POST /comment/:id

## 测试验证
- GET /api/reports ✔
- GET /api/reports/stats ✔
- POST /api/reports/:id/handle ✔ (dismiss → status=dismissed)
- POST /api/market/report ✔
- POST /api/pets/report ✔
- POST /api/teachers/report ✔
- POST /api/clubs/report ✔
- POST /api/activities/report ✔
- Server restart without crash ✔

## 涉及文件清单
- `config/database.js` — reports 建表
- `server.js` — 路由注册
- `routes/reports.js` — 统一举报路由
- `routes/wall.js` — 写作双表
- `routes/market.js` — 举报入口
- `routes/pets.js` — 举报入口
- `routes/teachers.js` — 举报入口 + import修复
- `routes/clubs.js` — 举报入口
- `routes/activities.js` — 举报入口
- `admin.html` — UI重写
- `admin/js/reports.js` — JS重写
- `admin/css/style.css` — 样式追加
