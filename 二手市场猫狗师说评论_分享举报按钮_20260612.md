# 二手市场/猫狗日记/师说评论 — 分享与举报按钮

## 目标
为二手市场、猫狗日记、师说三个模块的评论区域添加分享（📤）和举报（🚫）按钮，图标风格与校园墙评论保持一致（低透明度 emoji 图标，hover 高亮）。

## 修改文件

### 1. `api.js` — 新增 3 个举报 API 函数
- `API.marketReport(targetType, targetId, reason, detail)` → `POST /api/market/report`
- `API.petReport(targetType, targetId, reason, detail)` → `POST /api/pets/report`
- `API.teacherReport(targetType, targetId, reason, detail)` → `POST /api/teachers/report`

### 2. `app/js/wall.js` — 通用化举报 + 新增分享函数
- `showReportMenu(targetType, targetId, source)` 新增 third arg `source`（wall/market/pet/teacher），默认 'wall'，存到 `_reportSource` 变量
- `doReport` 按 `_reportSource` 分发到对应的 `API.xxxReport()` 函数
- 新增 `shareComment(source, itemName, commentText, author)` — 构建格式化分享文案 `📤 [来源] 作者：内容 ——来自校园圈`，复制到剪贴板（含 `execCommand('copy')` 降级）
- 导出 `window.shareComment = shareComment`

### 3. `app/js/market.js` — 主评论 + 嵌套回复
- 主评论 `comment-actions` 区域：回复按钮后、删除按钮前，插入 📤 分享（调用 `shareComment('market','商品',...)`） + 🚫 举报（调用 `showReportMenu('comment',id,'market')`）
- 嵌套回复 `comment-time` 行后：同样插入两个按钮（稍小尺寸，opacity:0.4）

### 4. `app/js/pets.js` — 猫狗日评评论
- `renderPetComments` 中每条评论末尾（媒体图片后）插入 📤 + 🚫 按钮行，source='pet'

### 5. `app/js/teachers.js` — 教师评价
- `openTeacherDetail` 中每条评价卡片末尾（媒体后）插入 📤 + 🚫 按钮行，source='teacher'

## 后端路由
已有的 `POST /api/market/report`、`POST /api/pets/report`、`POST /api/teachers/report` 已在 `routes/market.js`、`routes/pets.js`、`routes/teachers.js` 中实现，数据统一写入 `reports` 表。

## 验证
- 所有 5 个 JS 文件 `node --check` 语法检测通过
- `window.shareComment` 和 `window.showReportMenu` 均已通过 wall.js 导出到全局作用域
- 三个 API 函数已在 api.js 中定义
