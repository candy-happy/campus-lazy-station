# 管理端校园墙 - 发布者搜索

## 目标
在管理端校园墙帖子管理页面增加"发布者搜索"功能，可按昵称或手机号筛选帖子。

## 改动

### 后端 `routes/wall.js`
- `GET /admin/posts` 新增 `publisher` query 参数
- 匹配 `u.nickname LIKE ? OR p.phone LIKE ?`
- COUNT 查询按需 JOIN users 表（仅 publisher 存在时，避免无谓 JOIN）

### 前端 `admin.html`
- 在帖子搜索栏新增 `<input id="wallPostPublisher" placeholder="发布者昵称/手机号">`

### 前端 `admin/js/wall.js`
- `loadWallPosts()` 读取 `#wallPostPublisher` 值并作为 `publisher` 参数传给 API

## 验证
- `publisher=candy` → 19 posts ✅
- `keyword=校园` → 3 posts ✅
- 无参数 → 37 posts ✅
