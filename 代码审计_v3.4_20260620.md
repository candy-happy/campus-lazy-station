# 校园圈代码审计报告 v3.4
> 审计时间: 2026-06-20 11:46
> 审计范围: 全部代码 (server.js + 29路由 + 16前端模块 + 中间件 + 工具)

## 一、安全问题 (共 6 项)

### 🔴 Critical (2项)

**1. 验证码被注释禁用**
- 文件: `routes/auth.js:41-43`
- 问题: 用户登录验证码校验被注释掉，任何人都可无限尝试密码
- 风险: 暴力破解防护仅靠限流(5次/分钟)，但不同IP可绕过
- 修复: 取消注释，恢复验证码校验

**2. 豆包文生图接口无认证**
- 文件: `routes/doubao.js:19`
- 问题: `POST /api/ai/image/generate` 无 requireAuth，任何人可调用
- 风险: API Key 被滥用，产生费用
- 修复: 添加 `requireAuth` 中间件

### 🟠 High (2项)

**3. 校花校草结算接口无认证**
- 文件: `routes/campus-star.js:246`
- 问题: `POST /api/campus-star/settle` 无 requireAdmin，任何人可触发结算
- 修复: 添加 `requireAdmin` 中间件

**4. 教师评价媒体上传无认证**
- 文件: `routes/teachers.js:248`
- 问题: `POST /api/teachers/upload-media` 无 requireAuth，匿名用户可上传文件
- 修复: 添加 `requireAuth` 中间件

### 🟡 Medium (2项)

**5. 管理员登录明文密码兼容逻辑**
- 文件: `routes/auth.js:220-225`
- 问题: 管理员密码若非bcrypt格式则明文比对，存在时序攻击风险
- 修复: 迁移完成后删除明文兼容分支

**6. 前端 XSS 风险 (1处)**
- 文件: `app/js/discover.js:295`
- 问题: `innerHTML` 模板插值未转义活动数据
- 修复: 使用 `escHtml()` 转义动态内容

---

## 二、冗余问题 (共 4 项)

**1. .bak 文件未清理**
- `routes/chat.js.bak`
- `utils/response.js.bak`
- 建议: 删除，git 已有版本历史

**2. 根目录临时调试文件 (4个)**
- `check-nginx.md` — Nginx 诊断笔记
- `fix-error-handler.js` — 临时修复脚本
- `fix-https-redirect.sh` — HTTPS 重定向脚本
- `generate-pet-icon.js` — 宠物图标生成
- 建议: 移到 `scripts/` 或删除

**3. routes/ 中 36 处 console.log**
- 主要在 ai.js、wall.js、market.js
- 建议: 生产环境用统一 logger，或移除调试日志

**4. scripts/ 目录 35 个脚本**
- 大量为一次性迁移/修复脚本，未清理
- 建议: 保留有用的，归档或删除过时的

---

## 三、改进建议 (共 7 项)

### 架构优化

**1. CORS 配置硬编码**
- `server.js:30-40` 中 CORS 白名单硬编码在代码中
- 建议: 移到 `.env` 的 `CORS_ORIGINS` 配置（config 已支持但为空）

**2. 通用静态文件服务暴露根目录**
- `server.js:100` 的 `express.static(__dirname)` 暴露整个项目根目录
- 风险: 虽有敏感文件拦截，但仍可能泄露未预期的文件
- 建议: 仅静态服务 `public/`、`uploads/`、`app.html` 等明确目录

**3. uploads 定期清理用 setTimeout 而非 cron**
- `server.js:325` 用 `setTimeout(cleanupUploads, 24h)` 递归调用
- 问题: setTimeout 在大量连接时可能被延迟
- 建议: 使用 `setInterval` 或 node-cron

### 性能优化

**4. rateLimit 滑动窗口数组实现**
- `middleware/rateLimit.js` 用数组存储时间戳，每次请求 filter 全部
- 问题: 高并发下数组增长，filter 复杂度 O(n)
- 建议: 改用计数器+时间窗口重置法（O(1)）

**5. 骑手冻结检查每请求查 DB 缓存**
- `middleware/auth.js:63-72` 每30秒查一次 token_blacklist 表
- 建议: 已有内存缓存，可接受；但可加 `unref()` 避免阻止退出

### 代码质量

**6. wall.js 过大 (1035行/33路由)**
- 单文件包含帖子CRUD、评论、点赞、关注、举报、分享、搜索等
- 建议: 拆分为 wall-posts.js、wall-comments.js、wall-social.js

**7. market.js 过大 (806行/28路由)**
- 同上，可拆分为 market-items.js、market-orders.js、market-comments.js

---

## 四、总结评分

| 类别 | 评分 | 说明 |
|------|------|------|
| 安全性 | 7/10 | 验证码禁用+2接口无认证是主要扣分项 |
| 冗余度 | 7/10 | 有 .bak 和临时文件，但不影响运行 |
| 代码质量 | 8/10 | 结构清晰，统一错误码，参数化查询到位 |
| 性能 | 8/10 | SQLite WAL+缓存配置合理，限流分类完善 |
| 可维护性 | 7/10 | 两个大文件需拆分，整体模块化良好 |

**优先修复顺序:**
1. 恢复验证码（安全 Critical）
2. doubao.js 加认证（安全 Critical）
3. campus-star/settle 加管理员认证
4. teachers/upload-media 加认证
5. 清理 .bak 和临时文件
6. 修复 discover.js XSS
7. 拆分大文件
