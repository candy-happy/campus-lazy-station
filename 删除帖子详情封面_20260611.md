# 删除帖子详情封面

## 时间
2026-06-11 19:07

## 操作
移除帖子详情页的封面（pdHero）区域及其相关路径。

### 修改文件
1. **app.html** — 移除 `<div class="pd-hero" id="pdHero">`、`pd-back` 按钮、`pd-parallax` 类，改用标准子页面布局（带 sub-page-header 返回栏）
2. **app/css/style.css** — 删除所有 pd-hero/pd-hero::after/pd-back/pd-scroller 样式及视差滚动暗色模式 CSS，简化 pd-author-card
3. **app/js/wall.js** — 删除 Hero 背景图渲染代码块、initParallaxScroll() 函数及调用，调整作者卡 ⋯ 按钮为普通配色

### 新布局
帖子详情页现为标准子页面：sub-page-header(←帖子详情) → pd-author-card → wallDetailContent
